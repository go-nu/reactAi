txt 파일 내 라이브러리 한번에 설치
pip install -r requirements.txt

가상 환경 접속
.\.venv\Scripts\activate
or
Scripts까지 이동 후(cd ./.venv/Scripts) activate 실행
* 이후 작업 폴더로 복귀(cd ../../)



pull 이후 venv 생성
python -m venv .venv